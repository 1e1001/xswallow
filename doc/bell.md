# Audio bell
Replacement for [`xbelld`](https://gitlab.com/gi1242/xbelld) that can playback audio correctly.

If you need to run a command on bell ring, use `xbelld` instead.

## Platform support
Has no platform-specific dependencies, so should work on any platform with X.
